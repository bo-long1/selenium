[behave]
# Default settings for behave
# NOTE: For Allure reports with runner.py, results are converted from JSON
# To use Allure formatter directly, run: behave -f allure_behave.formatter:AllureFormatter -o allure_results

# Console appearance
color = true
show_timings = false
show_source = true
show_skipped = true
